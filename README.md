# NiKa_PH_duihuanma_mvp
# NiKa-C端兑换码分销平台-mvp版本 
1：调用 CDK 生成 / 核销 / 批次管理接口；切换角色 Token 测试权限；验证响应状态码  
2：模拟并发兑换（测响应时间）；批量生成 10 万 + CDK（测大数量处理）；遍历参数测试  
3：批量检查 CDK 格式 / 离散度；自动比对日志字段；生成测试数据  
4：查询 CDK 状态、核销日志表；验证数据更新正确性  
5：模拟 C 端用户在 App 内输入 CDK；验证前端提示与权益到账反馈，这个我们用C端的接口来验证
